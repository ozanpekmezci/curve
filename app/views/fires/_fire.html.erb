<article data-user-id="<%= fire.user_id %>">
  <div class="fire" data-id="<%= fire.id %>">

  	<p class="fire-body"><%=  fire.body %></p>
  	<p>Sent by:<%= link_to fire.user.name, fire.user %>, at: <%= comment.created_at.strftime('%H:%M:%S %-d %B %Y') %></p>

  </div>
</article>
